# 粒子对撞机模拟程序 (LHC Simulator)

一个基于ATLAS框架的粒子对撞机模拟系统，支持微信小程序和Web双端访问。

## 项目架构

### 前端
- **框架**: Taro (支持微信小程序 + Web)
- **技术栈**: React + TypeScript + Three.js (3D可视化)
- **位置**: `frontend/`

### 后端
- **语言**: C++
- **框架**: ATLAS (Pythia/Sherpa/MadGraph + Geant4 + Athena)
- **位置**: `backend/`

## 功能特性

- 🎯 粒子对撞事件生成（Pythia/Sherpa/MadGraph）
- 🔬 物理过程模拟（Geant4）
- 📊 数据分析和可视化（Athena）
- 📱 微信小程序支持
- 🌐 Web端支持
- 🎨 3D粒子轨迹可视化

## 快速开始

### 前置要求

#### 前端
- Node.js >= 16.0.0
- npm 或 yarn

#### 后端
- C++17 或更高版本
- CMake >= 3.15
- Geant4 >= 11.0
- ROOT (用于数据分析)
- Pythia8 (用于事件生成)

### 安装依赖

#### 前端
```bash
cd frontend
npm install
```

#### 后端
```bash
cd backend
mkdir build && cd build
cmake ..
make
```

### 运行项目

#### 前端开发
```bash
# 微信小程序
npm run dev:weapp

# Web端
npm run dev:h5
```

#### 后端服务
```bash
cd backend/build
./lhc_simulator
```

## 项目结构

```
.
├── frontend/          # Taro前端项目
│   ├── src/
│   │   ├── pages/     # 页面
│   │   ├── components/# 组件
│   │   ├── utils/     # 工具函数
│   │   └── services/  # API服务
│   └── config/        # Taro配置
├── backend/           # C++后端项目
│   ├── src/
│   │   ├── generator/ # 事件生成器（Pythia/Sherpa/MadGraph）
│   │   ├── simulation/# Geant4模拟
│   │   ├── analysis/  # Athena分析
│   │   └── api/       # API服务
│   ├── include/       # 头文件
│   └── CMakeLists.txt
└── docs/              # 文档

```

## 开发指南

详细开发文档请参考 [docs/](docs/) 目录。

## 许可证

MIT License
